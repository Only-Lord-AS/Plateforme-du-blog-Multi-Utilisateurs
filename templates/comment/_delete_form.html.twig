<form method="post" action="{{ path('app_comment_delete', {'id': comment.id, '_locale': app.request.locale}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comment.id) }}">
    <button class="btn">Delete</button>
</form>
